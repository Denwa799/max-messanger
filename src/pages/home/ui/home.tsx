import { SpacePatternBackground } from '@shared/ui';

export const Home = () => (
  <div className="relative h-full w-full overflow-hidden" aria-label="Область сообщений">
    <SpacePatternBackground className="absolute inset-0" />
  </div>
);
